from setuptools import setup, find_packages

setup(
    name='molecular_gcn',
    version='0.1',
    packages=find_packages(),
    description='A Graph Convolutional Networks for molecular data.',
)

